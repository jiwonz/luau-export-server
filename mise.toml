[tools]
"github:luau-lang/lute" = "latest"

[tasks.start]
description = "Start the server locally"
run = "lute src/server.luau"
env = { DEV = true }

[tasks.check]
description = "Run type checking and linting"
run = [
	"lute check src",
	"lute lint src"
]
